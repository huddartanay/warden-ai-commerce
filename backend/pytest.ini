[pytest]
testpaths = tests
addopts = -q
filterwarnings =
    # Third-party: starlette's TestClient still imports anyio.abc.BlockingPortal
    # for compatibility; noise, not ours.
    ignore:The anyio.abc.BlockingPortal alias is deprecated.*:DeprecationWarning
